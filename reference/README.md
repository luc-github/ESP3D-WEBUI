# Reference – copie de `src` avant retour à main

Ce dossier est une **copie de `src`** telle qu’elle était avant de remettre le code sur l’état de la branche `main`.

Il contient tout le travail fait après la baseline ~97 KB (filtres verboses, drag/drop + individualisation des panels, correctifs, etc.) pour pouvoir s’y référer si besoin.

- **Ne pas utiliser** ce dossier dans le build : le build lit `src`.
- **Consulter** les fichiers ici pour retrouver une implémentation ou comparer avec l’état actuel de `src`.
